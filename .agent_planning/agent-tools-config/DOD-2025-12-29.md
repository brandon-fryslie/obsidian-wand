# Definition of Done: Agent Configuration & Comparison

**Generated**: 2025-12-29

---

## P0-1: Per-Agent Tool Configuration

### Acceptance Criteria
- [ ] Agent interface has method to get its configured tools
- [ ] WandAgent configured with full tool set
- [ ] Agent's system prompt only includes its configured tools
- [ ] Settings allow configuring tools per agent type
- [ ] TypeScript compiles

---

## P0-2: Per-Agent LLM Configuration

### Acceptance Criteria
- [ ] Settings support per-agent LLM overrides (provider, model, temperature)
- [ ] Agent uses its specific LLM config (or inherits global if not set)
- [ ] Settings UI has per-agent LLM section
- [ ] TypeScript compiles

---

## P0-3: Multiple Agent Tabs

### Acceptance Criteria
- [ ] Can open multiple chat views (tabs)
- [ ] Each tab can be configured to use a different agent
- [ ] Tabs run independently (don't share state)
- [ ] User can visually compare outputs across tabs
- [ ] TypeScript compiles, build succeeds

---

## Sprint Complete When

1. [ ] All P0 acceptance criteria met
2. [ ] `pnpm run typecheck` passes
3. [ ] `pnpm run test` passes
4. [ ] `pnpm run build` succeeds
5. [ ] Manual test: Open 2 tabs with different agents, send same message, see different results
